import tools.waf

bld.stlib(source=bld.path.ant_glob('**/*.c'),
          target='libbtutil',
          includes=['.'],
          use=['pblibc_includes', 'pbl_includes'],
          env=bld.env.derive())

# vim:filetype=python
